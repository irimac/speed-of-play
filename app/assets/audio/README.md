# Audio Assets Placeholder

Add low-latency tick and round-start WAV files here:
- `tick.wav`: 1 Hz metronome click (≤150 ms length).
- `round_start.wav`: Distinct cue for entering a round.

Keep files mono 44.1 kHz for minimal overhead. Update `pubspec.yaml` if file names change.
